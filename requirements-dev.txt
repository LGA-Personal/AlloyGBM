# AlloyGBM developer dependencies.
#
# Install:  python -m pip install -r requirements-dev.txt
#
# This is the canonical set of Python packages required to:
#   - build the native extension (`maturin develop`)
#   - run the Python test suite (`pytest bindings/python/tests/`)
#   - exercise every test path (sklearn / pandas-backed tests)
#   - run the benchmark harness (`benchmarks/run_model_comparison.py`)
#
# Pinning policy:
#   - Build tooling (maturin) pinned to a SemVer range matching
#     pyproject.toml's build-system.requires so CI and local dev agree.
#   - Runtime deps (numpy) pinned to the same lower bound as
#     pyproject.toml's [project].dependencies.
#   - Test deps loose-bounded; bump as needed.

# Build
maturin>=1.7,<2.0

# Runtime
numpy>=1.24

# Test infra
pytest>=8.0
pytest-subtests>=0.13
pytest-cov>=7.1.0
coverage>=7.0

# Optional-but-tested integration paths
scikit-learn>=1.4
pandas>=2.0

# Benchmarks (cross-library comparison)
lightgbm>=4.0
xgboost>=2.0
catboost>=1.2

# Docs
sphinx>=7.0
sphinx-rtd-theme>=2.0
