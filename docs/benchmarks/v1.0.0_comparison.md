# AlloyGBM v1.0.0 Benchmark Results

Comparative results against XGBoost, LightGBM, and CatBoost, produced for the
v1.0.0 release.

| | |
|---|---|
| Date | 2026-08-31 |
| AlloyGBM | 1.0.0 |
| LightGBM | 4.6.0 |
| XGBoost | 3.2.0 |
| CatBoost | 1.2.10 |
| NumPy | 2.5.0 |
| Host | Apple silicon (arm64), 10 logical CPUs, macOS |
| Python | 3.13 |

Reproduce with:

```bash
python benchmarks/run_model_comparison.py --threads 1          # curated suite
python benchmarks/scale_comparison.py --rows 200000 1000000 --threads 1 10
```

> **Companion:** [`v1.0.0_deep_scaling.md`](v1.0.0_deep_scaling.md) sweeps the
> thread budget across deeper, many-tree configurations (216 cells, 4 libraries)
> to characterise scaling rather than single-point speed.

The raw records behind every number below are committed alongside this
document, including the environment and fairness settings each run used:
[`data/v1.0.0_curated_suite.json`](data/v1.0.0_curated_suite.json) (176
records) and [`data/v1.0.0_scale.json`](data/v1.0.0_scale.json) (32 records).

## Fairness controls

Every library is given the same work and the same resources. Each run records
its settings in the JSON output under `params.fairness` and
`params.environment`.

- **Equal compute budget.** `--threads N` is applied through each library's own
  knob (AlloyGBM/LightGBM/XGBoost `n_jobs`, CatBoost `thread_count`), and
  `OMP_NUM_THREADS`, `OPENBLAS_NUM_THREADS`, `MKL_NUM_THREADS`,
  `NUMEXPR_NUM_THREADS`, and `VECLIB_MAXIMUM_THREADS` are pinned to the same
  value so no runtime quietly takes more.
- **Equal hyperparameters.** Shared `n_estimators`, `learning_rate`,
  `max_depth`, seed, and 0.8/0.8 row/column subsampling. Two corrections are
  required for that equality to be real: LightGBM ignores `subsample` unless
  `subsample_freq >= 1`, and its `num_leaves` default of 31 would cap trees
  below the depth-wise peers' `2 ** max_depth`. Both are set explicitly.
- **Equal data.** The same arrays and the same split for every library; data
  preparation and metric computation are outside the timed region.

> **Pre-1.0 results are not comparable.** Before v1.0.0 the harness pinned the
> peer libraries to a single thread while leaving AlloyGBM unconstrained, so it
> used every core. Any speed comparison from an earlier run is inflated by
> roughly AlloyGBM's parallel speedup. The accuracy claims also shifted once
> LightGBM's bagging and leaf capacity were corrected.

## Why the curated suite is single-threaded

The curated scenarios span 142 to 40,000 rows. At that size, giving every
library 10 threads measures thread-spawn overhead rather than throughput: on
the reference host, LightGBM and XGBoost are **slower** multi-threaded than
single-threaded below roughly 40,000 rows — LightGBM by up to 20x on the
smallest scenarios.

| Scenario | Rows | AlloyGBM | LightGBM | XGBoost | CatBoost |
|---|---:|---:|---:|---:|---:|
| `wine_multiclass` | 142 | 0.96x | 0.05x | 0.05x | 3.18x |
| `dense_numeric` | 1,279 | 2.01x | 0.07x | 0.21x | 1.69x |
| `panel_time_series` | 5,618 | 1.42x | 0.16x | 0.37x | 2.34x |
| `california_housing` | 16,512 | 1.43x | 0.16x | 0.54x | 1.70x |
| `histogram_stress` | 40,000 | 1.85x | 0.63x | 1.91x | 3.29x |

*(fit-time speedup from 1 thread to 10 threads; below 1.0 means threading made
that library slower)*

Reporting a multi-threaded win at this scale would flatter AlloyGBM for a
reason unrelated to its algorithms. The suite is therefore run
single-threaded — a clean per-core comparison that is also the most
reproducible across machines — and the realistic multi-threaded case is
measured separately at 200k and 1M rows.

## Curated suite — accuracy (single-threaded)

Best value per row in **bold**.
#### Regression — RMSE

| Scenario | Rows | AlloyGBM | LightGBM | XGBoost | CatBoost |
|---|---:|---:|---:|---:|---:|
| `abalone_regression` | 3,341 | 2.2517 | 2.2618 | 2.2695 | **2.2218** |
| `bike_sharing` | 13,903 | **68.7193** | 69.9969 | 70.9532 | 75.8023 |
| `california_housing` | 16,512 | 0.4933 | **0.4670** | 0.4690 | 0.5083 |
| `dense_numeric` | 1,279 | 0.5482 | 0.5515 | **0.5429** | 0.5522 |
| `dow_jones_financial` | 570 | **3.3944** | 3.5559 | 3.5226 | 3.4517 |
| `histogram_stress` | 40,000 | **0.1803** | 0.3613 | 0.3649 | 0.3600 |
| `panel_time_series` | 5,618 | 32.5121 | 31.7002 | 32.4060 | **31.1732** |

#### Classification — ACCURACY

| Scenario | Rows | AlloyGBM | LightGBM | XGBoost | CatBoost |
|---|---:|---:|---:|---:|---:|
| `adult_income` | 24,129 | 0.8676 | **0.8697** | 0.8694 | 0.8618 |
| `breast_cancer` | 455 | 0.9474 | **0.9737** | 0.9386 | 0.9649 |
| `synthetic_classification` | 40,000 | 0.9842 | 0.9831 | 0.9840 | **0.9858** |

#### Multiclass Classification — ACCURACY

| Scenario | Rows | AlloyGBM | LightGBM | XGBoost | CatBoost |
|---|---:|---:|---:|---:|---:|
| `digits_multiclass` | 1,437 | 0.9833 | **0.9889** | 0.9750 | 0.9722 |
| `synthetic_multiclass` | 8,000 | 0.8060 | 0.8045 | 0.7995 | **0.8070** |
| `wine_multiclass` | 142 | **0.9722** | **0.9722** | **0.9722** | **0.9722** |

#### Ranking — NDCG_10

| Scenario | Rows | AlloyGBM | LightGBM | XGBoost | CatBoost |
|---|---:|---:|---:|---:|---:|
| `california_ranking` | 13,505 | 0.7674 | 0.7430 | **0.7727** | 0.7461 |
| `synthetic_ranking` | 4,000 | **1.0000** | 0.9982 | 0.9964 | 0.9985 |

#### Fit time, single-threaded (seconds)

| Scenario | Rows | AlloyGBM | LightGBM | XGBoost | CatBoost |
|---|---:|---:|---:|---:|---:|
| `abalone_regression` | 3,341 | 0.109 | **0.022** | 0.033 | 0.093 |
| `adult_income` | 24,129 | 0.402 | 0.141 | **0.080** | 0.262 |
| `bike_sharing` | 13,903 | 0.302 | 0.063 | **0.055** | 0.165 |
| `breast_cancer` | 455 | 0.245 | **0.023** | 0.023 | 0.396 |
| `california_housing` | 16,512 | 0.222 | **0.083** | 0.093 | 0.263 |
| `california_ranking` | 13,505 | 3.370 | **0.248** | 0.473 | 7.341 |
| `dense_numeric` | 1,279 | 0.133 | **0.014** | 0.032 | 0.074 |
| `digits_multiclass` | 1,437 | 0.532 | 0.221 | **0.167** | 0.861 |
| `dow_jones_financial` | 570 | 0.142 | **0.011** | 0.041 | 0.126 |
| `histogram_stress` | 40,000 | 0.863 | **0.410** | 0.544 | 0.800 |
| `panel_time_series` | 5,618 | 0.121 | **0.029** | 0.061 | 0.191 |
| `synthetic_categorical` | 0 | **0.000** | **0.000** | **0.000** | **0.000** |
| `synthetic_classification` | 40,000 | 0.936 | **0.439** | 0.486 | 1.095 |
| `synthetic_multiclass` | 8,000 | 1.806 | **0.549** | 0.813 | 1.383 |
| `synthetic_ranking` | 4,000 | 0.283 | 0.082 | **0.072** | 0.510 |
| `wine_multiclass` | 142 | 0.111 | **0.010** | 0.010 | 0.149 |

## Large-scale, multi-threaded (200k and 1M rows, 40 features, 100 rounds)

Synthetic regression and binary classification, all four libraries at the same
thread budget. This is the realistic deployment case: datasets large enough
that parallelism pays for everyone.

### 1,000,000 rows — regression (RMSE)

| Model | Fit, 1 thread | Fit, 10 threads | Parallel speedup | Predict | RMSE |
|---|---:|---:|---:|---:|---:|
| AlloyGBM | 12.18 s | 7.01 s | 1.74x | 0.103 s | 2.4337 |
| LightGBM | **6.81 s** | **2.41 s** | 2.83x | 0.096 s | 2.4322 |
| XGBoost | 8.12 s | 8.13 s | 1.00x | 0.107 s | 2.4341 |
| CatBoost | 8.11 s | 1.94 s | **4.19x** | **0.013 s** | **2.4181** |

### 1,000,000 rows — binary classification (accuracy)

| Model | Fit, 1 thread | Fit, 10 threads | Parallel speedup | Predict | Accuracy |
|---|---:|---:|---:|---:|---:|
| AlloyGBM | 12.68 s | 7.91 s | 1.60x | 0.127 s | 0.6637 |
| LightGBM | **7.44 s** | **2.96 s** | 2.52x | 0.110 s | 0.6646 |
| XGBoost | 8.17 s | 8.19 s | 1.00x | 0.118 s | 0.6645 |
| CatBoost | 11.32 s | 2.83 s | **4.00x** | **0.038 s** | **0.6650** |

Accuracy at scale is effectively tied — all four land within 0.7% RMSE and
0.2 percentage points of accuracy. The separation is in fit time.

## What the results say

**Accuracy.** Across the 15 curated scenarios AlloyGBM wins 5 and places in the
top two on 11. It is strongest on `histogram_stress` (0.180 RMSE against
0.360–0.365 for all three peers — a 2x margin), `bike_sharing`,
`dow_jones_financial`, and both ranking scenarios. It trails on
`panel_time_series` (last of four, 4.3% behind CatBoost),
`california_housing`, `breast_cancer`, and `adult_income`. At 200k–1M rows,
accuracy is effectively tied with all three peers.

**Fit speed.** This is AlloyGBM's weak axis, and the gap has two distinct
causes:

- *Fixed per-round overhead* dominates on small data. On the curated suite
  (median 5.6k rows) LightGBM and XGBoost fit roughly 3–4x faster per core.
  At 200k rows that shrinks to about 1.3x, and at 1M rows to 1.5–1.8x — the
  per-row work is competitive; the constant is not.
- *Parallel scaling* is the larger structural gap. AlloyGBM reaches 1.6–1.8x
  on 10 cores where LightGBM reaches 2.5–2.8x and CatBoost 4.0–4.2x. This is
  the headline post-1.0 performance theme (see
  [limitations](../limitations.md)). XGBoost showed no scaling at all on this
  host and workload (1.00x), so AlloyGBM is faster than XGBoost multi-threaded
  at 1M rows despite being slower single-threaded.

**Prediction speed.** Competitive: comparable to LightGBM and XGBoost at scale
and markedly faster than LightGBM single-threaded on the curated suite
(median 4.8x). CatBoost's oblivious trees predict fastest by a wide margin.

## Effect of the v1.0.0 default changes

Two v1.0.0 default changes were validated on this suite rather than asserted:

- `lambdarank_normalize=True` (matching LightGBM's `lambdarank_norm`) lifted
  `california_ranking` NDCG@10 from 0.6547 to **0.7674**, moving AlloyGBM from
  last to second and ahead of both LightGBM (0.7430) and CatBoost (0.7461).
  On uniform 50-document groups it improved NDCG@10 from 0.9649 to 0.9697.
- `lambdarank_truncation_level=30` (also matching LightGBM) is a
  speed-for-quality trade on datasets with large query groups. On
  `california_ranking` (median 120, max 3,307 documents per query), raising it
  to `None` gains a further ~0.04 NDCG@10 at ~1.5x the fit time. Users with
  large groups should consider raising it.
